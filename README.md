# AWS CDK Blueprints vs Custom Constructs

We compare AWS CDK Blueprints to Custom L3 constructs, with examples in TypeScript and the AWS CDK

![header](./docs/images/header.png)

> Note: If you choose to deploy these resources you may be charged for usage.

You can find the associated article here: https://medium.com/@leejamesgilmore/aws-cdk-blueprints-vs-custom-constructs-vs-aspects-164304251e68

### Deploying the Solution

1. cd into either the `blueprints` or `custom-constructs` folder.
2. run `npm i`.
3. run `npm run deploy:develop`.

### Removing the Solution

To remove the solution run the following:

1. In the `blueprints` or `custom-constructs` folder run the following `npm run remove:develop`.
